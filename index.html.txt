<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>陳泓諭 Hong-Yu Chen | 角力摔技專班</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        :root {
            --primary: #d4af37; /* 冠軍金 */
            --danger: #ff4d4d;
            --bg: #05070a;
            --card-bg: #111827;
            --text: #ffffff;
        }

        * { box-sizing: border-box; margin: 0; padding: 0; font-family: "PingFang TC", "Microsoft JhengHei", sans-serif; }
        body { background-color: var(--bg); color: var(--text); line-height: 1.6; overflow-x: hidden; scroll-behavior: smooth; }

        /* 1. 導航與懸浮 */
        .social-float {
            position: fixed; right: 20px; bottom: 30px; z-index: 1000;
            display: flex; flex-direction: column; gap: 15px;
        }
        .social-btn {
            width: 60px; height: 60px; border-radius: 50%; display: flex;
            justify-content: center; align-items: center; color: white;
            text-decoration: none; font-size: 24px; box-shadow: 0 4px 15px rgba(0,0,0,0.5);
            transition: 0.3s;
        }
        .bg-line { background: #06C755; }
        .bg-ig { background: linear-gradient(45deg, #f09433 0%, #e6683c 25%, #dc2743 50%, #cc2366 75%, #bc1888 100%); }
        .social-btn:hover { transform: scale(1.1) rotate(5deg); }

        /* 2. 首屏 Hero - 改用教練海報作為背景 */
        .hero {
            height: 100vh; background: linear-gradient(rgba(0,0,0,0.5), var(--bg)), url('hero.jpg');
            background-size: cover; background-position: top center;
            display: flex; flex-direction: column; justify-content: flex-end; align-items: center; 
            text-align: center; padding-bottom: 100px;
        }
        .hero h1 { font-size: clamp(2.5rem, 8vw, 5rem); font-weight: 900; letter-spacing: 2px; text-shadow: 2px 2px 20px rgba(0,0,0,0.8); }
        .hero p { font-size: 1.2rem; color: var(--primary); font-weight: bold; margin-top: 10px; }

        /* 3. 資歷區塊 */
        section { padding: 100px 20px; max-width: 1200px; margin: 0 auto; }
        .grid-about { display: grid; grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); gap: 50px; align-items: center; }
        .stats h2 { font-size: 2.5rem; margin-bottom: 30px; color: var(--primary); border-bottom: 2px solid var(--primary); display: inline-block; }
        .stat-list { list-style: none; }
        .stat-list li { margin-bottom: 20px; font-size: 1.1rem; border-left: 4px solid var(--danger); padding-left: 15px; }
        .stat-list li strong { color: var(--primary); font-size: 1.3rem; display: block; }

        /* 4. 影片區 */
        .video-box { text-align: center; background: #000; padding: 60px 20px; border-radius: 30px; margin: 50px 0; }
        video { width: 100%; max-width: 900px; border-radius: 15px; box-shadow: 0 0 40px rgba(212, 175, 55, 0.3); }

        /* 5. 圖片牆 */
        .gallery { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 20px; }
        .gallery img { width: 100%; height: 350px; object-fit: cover; border-radius: 15px; transition: 0.5s; cursor: pointer; }
        .gallery img:hover { transform: scale(1.03); filter: brightness(1.2); }

        /* 6. CTA 按鈕 */
        .cta-box { text-align: center; padding: 120px 20px; background: linear-gradient(to bottom, var(--bg), #1a1a1a); }
        .main-btn {
            background: linear-gradient(45deg, var(--danger), var(--secondary));
            color: white; padding: 25px 60px; font-size: 1.6rem; font-weight: 800;
            border: none; border-radius: 100px; cursor: pointer; transition: 0.3s;
            text-decoration: none; display: inline-block; box-shadow: 0 10px 40px rgba(255, 77, 77, 0.4);
        }
        .main-btn:hover { transform: scale(1.1); filter: brightness(1.1); }

        /* 滑動動畫 */
        .reveal { opacity: 0; transform: translateY(40px); transition: 1s ease-out; }
        .reveal.active { opacity: 1; transform: translateY(0); }
    </style>
</head>
<body>

    <!-- 社交懸浮鈕 -->
    <div class="social-float">
        <a href="https://www.instagram.com/chen.02.08/" target="_blank" class="social-btn bg-ig">
            <i class="fab fa-instagram"></i>
        </a>
        <a href="https://line.me/ti/p/你的LINE連結" target="_blank" class="social-btn bg-line">
            <i class="fab fa-line"></i>
        </a>
    </div>

    <header class="hero">
        <h1 class="reveal">陳泓諭 角力專班</h1>
        <p class="reveal">RELENTLESS WORK. UNWAVERING PURPOSE.</p>
    </header>

    <section id="about">
        <div class="grid-about">
            <div class="stats reveal">
                <h2>戰績與資歷</h2>
                <ul class="stat-list">
                    <li><strong>全運會角力兩屆冠軍</strong>2021, 2023 National Games Champion</li>
                    <li><strong>全大運五連霸金牌</strong>2020-2026 Intercollegiate Champion</li>
                    <li><strong>全國普及運動會冠軍</strong>2024 Taiwan National Popular Games</li>
                    <li><strong>國際賽事資歷</strong>U15-U20 亞洲錦標賽代表隊</li>
                </ul>
            </div>
            <div class="reveal">
                <img src="medal.jpg" style="width:100%; border-radius:20px; border:1px solid var(--primary);">
            </div>
        </div>
    </section>

    <section class="reveal">
        <h2 style="text-align: center; color: var(--primary); margin-bottom: 30px;">掌控重心的藝術</h2>
        <div class="video-box">
            <video controls muted autoplay loop poster="hero.jpg">
                <source src="video1.mp4" type="video/mp4">
            </video>
        </div>
    </section>

    <section class="reveal">
        <h2 style="text-align: center; color: var(--primary); margin-bottom: 40px;">訓練實錄與氛圍</h2>
        <div class="gallery">
            <img src="team.jpg" alt="猛男訓練營">
            <img src="hero.jpg" alt="個人特寫">
            <img src="medal.jpg" alt="榮耀時刻">
        </div>
    </section>

    <section class="cta-box">
        <h2 class="reveal" style="font-size: 2.5rem; margin-bottom: 30px;">想讓世界在面前翻轉嗎？</h2>
        <a href="https://www.instagram.com/chen.02.08/" class="main-btn reveal">私訊 IG 預約體驗</a>
        <p style="margin-top: 20px; color: #64748b;">IG: @chen.02.08</p>
    </section>

    <script>
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) entry.target.classList.add('active');
            });
        }, { threshold: 0.1 });
        document.querySelectorAll('.reveal').forEach(el => observer.observe(el));
    </script>
</body>
</html>